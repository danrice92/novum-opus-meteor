{
  "name": "novum-opus",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --once --full-app --driver-package meteortesting:mocha --port 3100",
    "watch-test": "TEST_WATCH=1 meteor test --full-app --driver-package meteortesting:mocha --port 3100",
    "visualize": "meteor --production --extra-packages bundle-visualizer"
  },
  "dependencies": {
    "@babel/runtime": "7.0.0-beta.55",
    "chai": "^4.1.2",
    "enzyme": "^3.5.0",
    "meteor-node-stubs": "^0.4.1",
    "react": "^16.4.2",
    "react-dom": "^16.4.2"
  },
  "meteor": {
    "mainModule": {
      "client": "client/main.js",
      "server": "server/main.js"
    },
    "testModule": "tests/main.js"
  }
}
